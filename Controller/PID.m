function [outputArg1,outputArg2] = PID(currentState, nextGoal)
%PID takes current state and next goal
%   both in the format (x, y, theta) in a row vector

% constants
forward_gain_P = 1;
angular_gain_P = 0.3;







end

